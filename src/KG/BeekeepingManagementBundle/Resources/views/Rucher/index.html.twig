{# src/KG/BeekeepingManagementBundle/Resources/views/Rucher/index.html.twig #}

{% extends "KGBeekeepingManagementBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Ruchers{% endblock %}

{% block rucher %}class="active"{% endblock %}

{% block body %}
    <strong>Rucher</strong>  
    <a title="Créer un rucher" data-toggle="modal" href="{{ path('kg_beekeeping_management_add_rucher') }}" class="btn btn-primary pull-right"><span class="glyphicon glyphicon-plus-sign"></span> Créer un rucher</a>
    <hr>
    
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <span class="glyphicon glyphicon-list"></span> Liste des ruchers
                </div>
                <div class="panel-body">
                    <ul class="list-group">
                        {% for rucher in ruchers %}
                            <li class="list-group-item">
                                {{ rucher.nom }}
                                <div class="pull-right action-buttons">
                                    <a title="Afficher le rucher" href="{{ path('kg_beekeeping_management_view_rucher', { 'id': rucher.id }) }}" class="btn btn-xs btn-success pull-right"><span class="glyphicon glyphicon-eye-open"></span> Afficher</a>
                                </div>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
                <div class="panel-footer">
                    <div class="row">
                        <div class="col-md-6">
                            <span class="label label-info">{{ nbRuchers }} ruchers</span>
                        </div>
                        <div class="col-md-6">
                            {% include "KGBeekeepingManagementBundle::pagination.html.twig" %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}