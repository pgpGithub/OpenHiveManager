{# src/KG/BeekeepingManagementBundle/Resources/views/LieuStock/index.html.twig #}

{% extends "::layout.html.twig" %}

{% block title %}{{ parent() }} - Lieux de Stockage{% endblock %}

{% block lieuStock %}class="active"{% endblock %}

{% block body %}
    <strong>Stock</strong>  
    <a title="Créer un lieu de Stockage" data-toggle="modal" href="{{ path('kg_beekeeping_management_add_lieuStock') }}" class="btn btn-primary pull-right"><span class="glyphicon glyphicon-plus-sign"></span> Créer un lieu de stockage</a>
    <hr>
    
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <span class="glyphicon glyphicon-list"></span> Liste des lieux de stockage
                </div>
                <div class="panel-body">
                    <ul class="list-group">
                        {% for lieuStock in lieuStocks %}
                            <li class="list-group-item">
                                {{ lieuStock.nom }}
                                <div class="pull-right action-buttons">
                                    <a title="Afficher le lieuStock" class="btn btn-xs btn-success pull-right"><span class="glyphicon glyphicon-eye-open"></span> Afficher</a>
                                </div>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
                <div class="panel-footer">
                    <div class="row">
                        <div class="col-md-6">
                            <span class="label label-info">{{ nbLieuStocks }} lieux de lieuStockage</span>
                        </div>
                        <div class="col-md-6">
                            {% include "::pagination.html.twig" %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}