Un message vous a été envoyé par {{ contact.prenom }} {{ contact.nom }} à {{ "now" | date("Y-m-d H:i") }}.

Adresse du contact : {{ contact.email }}
Sujet : {{ contact.sujet.libelle }}
Message :
{{ contact.message }}